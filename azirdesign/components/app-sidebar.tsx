"use client"

interface AppSidebarProps {
  meta?: any
}

export function AppSidebar({ meta }: AppSidebarProps) {
  return (
    <div>
      <div>Sidebar</div>
      {/* Add more UI as needed */}
    </div>
  )
}
